# Set comprehensions are created in much the same way as dictionary comprehensions.
# Python set is much like a mathematical set in that it doesn’t have any repeated elements.

my_list = [1, 2, 2, 3, 4, 5, 5, 7, 8]
my_set = set(my_list)
print(my_set) # {1, 2, 3, 4, 5, 7, 8}